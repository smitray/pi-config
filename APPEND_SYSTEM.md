## Web Access
When the user wants to search the web, use `web-search` (SearXNG). Prefer `tf_search` (TinyFish) if `PI_TINYFISH_API_KEY` is set.
For fetching URLs, use `web-fetch` (Crawl4AI) or `tf_fetch` (TinyFish). No need to ask — just do it.
